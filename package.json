{
  "name": "sql-playground",
  "version": "0.1.0",
  "private": true,
  "description": "Interactive CLI that spins up throwaway PostgreSQL/MySQL containers and drops you into their native SQL client.",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@inquirer/prompts": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
